# 01: 必填校验按节点字段权限分步执行（后端）

**仓库：hive-admin-go**（前端经核实已按目标语义实现，本工单零前端改动）

**What to build:** 发起流程时，"请填写XX"这类必填报错只针对发起节点可编辑的字段（未配置默认可编辑）；审批同意时，必填校验只针对当前节点显式设为可编辑的字段，且审批人未修改任何字段直接同意时同样校验；隐藏/只读的必填字段在到达将其设为可编辑的节点前不拦截流转。已提交非空值的格式校验维持全量。同批重写运行规则文档的两条规则并更新前端设计器 UI 文档的旧警告。

**Blocked by:** None（can start immediately）

**Status:** ready-for-agent

- [ ] 表单值校验函数支持"必填生效字段集合"参数：集合内字段必填生效，集合外豁免必填（含 selectRequired）；空集合语义为全部必填生效，既有调用与测试不破坏
- [ ] 发起：从画布开始节点解析字段权限（未配置默认可编辑），必填校验限可编辑字段；隐藏必填字段的流程可正常发起（本次问题的直接验收场景）
- [ ] 审批同意：必填校验限当前节点显式可编辑字段（未配置默认只读、豁免必填）；空变更（未修改任何字段直接同意）同样校验本节点可编辑必填；字段变更仍只允许可编辑字段
- [ ] 拒绝路径不校验表单（现状不变）；格式类规则对非空值维持全量校验
- [ ] 既有校验测试同步新参数并补三个行为断言：隐藏必填发起通过、可编辑必填空变更同意被拦、只读必填不拦
- [ ] WF-RUN-002、WF-RUN-022 重写；前端 definition-ui.md 旧警告（"发起节点不应隐藏必填字段否则被拒"）改为新语义说明
- [ ] `gofmt`、`go build ./...`、`go vet ./...`、`go test ./...` 通过
