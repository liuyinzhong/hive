# 患者管理上下文

用于统一患者主档案相关业务术语，明确患者管理与后续诊疗、收费等业务的边界。

## 语言

**患者**：接受或计划接受医疗服务的个人。患者与系统登录账号、医生和医疗机构是不同的业务概念。

_避免_：用户、客户、会员

**患者主档案**：用于识别和维护患者基础身份及联系信息的核心档案；第一期仅建立完整实名档案，并在全系统范围内共享。

_避免_：病历、就诊记录、诊疗档案

**患者编号**：由医疗机构系统为患者主档案分配且不可变更的稳定内部编号。

**身份凭证**：用于核验患者身份的证件类型及证件号码，是第一期完整实名患者档案的必要信息；第一期支持居民身份证、护照、港澳居民来往内地通行证、台湾居民来往大陆通行证和其他证件。

**患者敏感信息**：患者姓名、证件号码和手机号。此类信息在普通患者列表中不以完整形式展示。

**手机号**：中国大陆 11 位手机号码，是第一期患者主档案的必要联系信息，但可以由多个患者共同使用。

**紧急联系人**：患者无法联系或需要协助时，用于联系的指定人员及其关系和联系方式；不属于患者本人信息。

**重复患者档案**：多个患者主档案实际指向同一名患者的情况。姓名、性别和出生日期相同只能作为疑似重复依据，不能单独证明身份相同；同一身份凭证在全系统只能归属于一个保留档案。

**档案停用**：患者主档案不再参与新的业务选择，但档案本身及其历史关联仍然保留。

_避免_：删除患者、物理删除

**档案更正**：对患者主档案当前信息进行修正，不等同于创建新的患者档案。

## 挂号预约语言

**挂号单**：患者为获得一次医疗服务而建立的核心业务单据，统一关联患者、科室、医生、排班、挂号类型和费用；现场挂号和预约挂号均使用同一业务单据。

**预约**：挂号单的来源或预约方式，表示患者在计划就诊时间前预先申请医疗服务，不单独形成预约单。

**签到**：患者到达医疗机构后对挂号单进行到诊确认的业务动作，是挂号单的后续状态，不创建新的业务单据。

**挂号患者**：挂号单所关联的已存在且处于启用状态的患者主档案；挂号过程中不临时创建患者档案。

**号源时段**：排班下可被挂号单选择的具体时间段，具有独立的号源总数、已占用数和剩余数；挂号单必须关联具体号源时段。

**号源占用**：挂号单创建成功时对所选号源时段执行的数量占用；占用与挂号单创建在同一业务操作中完成，挂号单取消后释放。

**挂号费用快照**：挂号单创建时记录的挂号费用金额、费用规则编号及规则版本；后续挂号费规则变化不影响既有挂号单的费用。

**挂号初始状态**：挂号单创建成功后进入“待支付”状态；首期不接入实际支付渠道，但保留支付状态，为后续收费结算预留边界。

**挂号状态码**：使用稳定数值编码：`0（待支付）`、`10（已支付）`、`30（已签到）`、`50（已完成）`、`60（已取消）`、`70（爽约）`、`80（发起退款）`、`90（退款中）`、`100（退款完成）`；状态先在前后端枚举中定义，状态名称、合法流转、权限和号源/退款副作用由代码明确处理。新增状态时必须同步补充前后端枚举和对应业务逻辑，不依赖数值大小直接推断是否合法。

**待支付号源占用**：待支付状态的挂号单继续占用已选择的号源时段；首期不设置自动超时取消，只有执行取消挂号后才释放号源。

**确认支付**：首期由操作人员手动确认窗口或线下收款，将挂号单从 `0（待支付）` 流转为 `10（已支付）`；该动作不接入在线支付渠道。

**取消挂号**：首期仅允许状态为 `0（待支付）` 的挂号单取消，并流转为 `60（已取消）`；取消成功后释放号源。状态为 `10（已支付）` 的挂号单不直接进入取消状态，是否进入退款流程另按退款规则确定。

**签到**：仅允许状态为 `10（已支付）` 的挂号单签到；签到成功后流转为 `30（已签到）`，待支付挂号单不能进入签到流程。

**就诊完成**：签到后的挂号单由操作人员在本次就诊结束后手动流转为 `50（已完成）`；`50` 是正向流程终点，挂号模块只维护挂号生命周期，不维护诊疗内容。

**爽约**：号源结束后，状态为 `10（已支付）` 且未签到的挂号单可由操作人员手动流转为 `70（爽约）`；首期不使用定时任务自动处理，是否退款另按退款规则确定。

**挂号终态**：`50（已完成）`、`60（已取消）`、`70（爽约）` 和 `100（退款完成）` 为终态，普通操作不允许回退；如需纠正，后续单独设计受控的状态更正流程。

**退款流程**：退款作为挂号单生命周期的反向流程，按 `80（发起退款）`、`90（退款中）`、`100（退款完成）` 流转。

**退款发起条件**：仅状态为 `10（已支付）` 的挂号单允许发起退款，不设置距离号源开始时间的限制；退款发起后按 `10 → 80 → 90 → 100` 流转。

**退款状态推进**：首期由操作人员依次手动触发 `80（发起退款）`、`90（退款中）` 和 `100（退款完成）`；后续接入第三方支付后，再由第三方回调推进退款状态。

**退款金额**：退款金额固定等于挂号单创建时保存的费用快照金额；首期只支持全额退款，不支持部分退款、差额退款或手续费扣除。

**号源释放**：`0 → 60（已取消）` 时立即释放号源；退款处于 `80（发起退款）` 或 `90（退款中）` 时仍占用号源，只有进入 `100（退款完成）` 后才释放号源。

**取消与退款原因**：取消挂号和发起退款时必须填写业务原因，原因写入对应的挂号单生命周期记录，不在挂号主表增加专用原因字段。

**状态流转接口**：按业务动作而不是按状态设计接口；确认支付、取消、签到、完成、爽约和退款各阶段分别提供动作接口，并分别配置权限码。禁止提供可由调用方直接传入任意目标状态的通用状态修改接口。

**生命周期记录展示**：挂号详情以只读时间线展示挂号单生命周期记录，包括状态变化、操作时间、操作人、取消或退款原因和退款金额；不提供修改或删除生命周期记录的功能。

**初始生命周期记录**：创建挂号单时立即新增第一条生命周期记录，记录从空状态到 `0（待支付）` 的创建过程、创建人和创建时间；后续每次状态变化均新增一条记录。

**挂号详情接口**：挂号详情接口一次返回挂号主数据和完整生命周期记录，前端不单独请求生命周期记录接口。

**挂号创建入参边界**：创建接口仅接收 `patientId`、`slotId`、`registrationMethod` 和可选 `remark`；排班、科室、医生、就诊时间、挂号类型、费用快照、挂号编号及患者快照均由服务端根据当前数据生成。号源占用、挂号创建和初始生命周期记录在同一事务中完成。

**状态操作响应**：确认支付、取消、签到、完成、爽约和退款各阶段操作接口成功后，直接返回更新后的挂号详情及完整生命周期记录，前端不再额外请求详情接口。

**状态流转矩阵**：仅允许 `0（待支付）→10（已支付）`、`0→60（已取消）`、`10→30（已签到）`、`10→70（爽约）`、`10→80（发起退款）`、`30→50（已完成）`、`80→90（退款中）` 和 `90→100（退款完成）`；`50、60、70、100` 为终态，其它跳转和回退均禁止。

**列表操作展示**：挂号列表仅在当前状态允许对应动作且用户拥有该动作权限码时展示操作按钮；前端控制展示，后端再次校验状态和权限，终态不展示后续操作。

**挂号列表查询**：首期支持按挂号编号、患者编号/姓名/手机号、就诊日期范围、科室、医生、挂号类型、挂号方式和挂号状态查询。

**首期范围**：首期不提供挂号单打印和导出，先实现挂号创建、列表查询、详情查看、生命周期状态流转和退款流程。

**挂号单保留策略**：挂号单不提供删除接口和删除按钮；即使取消、爽约或退款完成，也保留挂号主记录及完整生命周期记录。

**挂号查看权限**：挂号列表和挂号详情分别使用独立权限码；详情权限同时控制挂号详情和生命周期记录查看，状态操作另行使用各自权限码。

**挂号权限码**：使用 `medical:registration:list`、`medical:registration:detail`、`medical:registration:create`、`medical:registration:confirmPayment`、`medical:registration:cancel`、`medical:registration:checkIn`、`medical:registration:noShow`、`medical:registration:refundStart`、`medical:registration:refundProcess` 和 `medical:registration:refundComplete`，权限仅按权限码判断；完成接诊由医生工作台正式接口处理。

**挂号方式**：挂号单的来源方式，首期通过字典区分 `0（现场挂号）` 和 `10（预约挂号）`；两种方式共用同一挂号单模型，不创建独立预约单。

**挂号类型/号别**：排班和号源的业务属性；创建挂号单时由所选号源自动带出，不允许操作人员临时修改，具体类型通过字典维护。

**挂号方式枚举**：使用前后端枚举维护挂号来源方式，`0` 表示现场挂号，`10` 表示预约挂号；挂号方式不是管理员动态配置字典。挂号类型/号别仍通过字典维护。

**患者信息快照**：挂号单除关联 `patientId` 外，还保存创建时的患者姓名、性别、出生日期、证件类型、证件号码和手机号；历史挂号展示不随患者主档案后续修改而改变，快照字段仍按敏感数据权限控制。

**排班号源信息快照**：挂号单保存 `departmentId`、`doctorId`、`scheduleId`、`slotId` 等关联，并保存创建时的科室名称、医生名称、就诊日期、开始时间、结束时间和挂号类型；费用信息另按费用快照保存。

**重复挂号约束**：同一患者在同一号源时段只能存在一张未取消挂号单；已支付挂号单首期不可取消，因此也参与重复校验。

**同日多科室挂号**：同一患者在同一天可以为不同科室、不同医生或不同号源时段创建多张挂号单；不以患者和日期作为全局唯一条件。

**可挂号号源**：`canBook` 是后端根据排班状态、号源开始时间和剩余号源计算出的快捷标志；只有 `canBook=true` 的具体号源时段允许创建挂号，服务端创建时仍需重新校验，不信任前端传值。

**挂号编号**：使用 `base_code_sequence` 按业务类型递增生成；序列类型为 `REGISTRATION`，前缀为 `REG`，数字部分长度为 6 位，格式如 `REG000001`，不包含日期且不复用已分配编号。

**挂号患者选择**：挂号模块直接复用患者列表接口选择患者，不新增独立患者检索接口，也不在挂号模块单独处理脱敏；患者列表接口返回的字段和权限行为由患者模块统一负责。挂号时仍只允许选择已启用的患者档案。

**挂号单不可变字段**：挂号单创建后不提供通用编辑；患者、号源、挂号方式、挂号类型、就诊时间和费用均不可直接修改，只允许通过明确的状态操作推进。信息选错时重新创建挂号单。

**挂号备注**：挂号单支持可选备注，仅允许在创建时填写，创建后不可编辑；备注不参与状态、费用和号源计算。

**支付确认信息**：手动确认支付时通过挂号单生命周期记录保存确认支付时间和确认人；首期不记录支付渠道、支付流水号等支付平台信息。

**挂号单生命周期记录**：记录挂号单每次业务状态流转的原状态、新状态、发生时间、操作人和必要备注；状态流转信息不在挂号主表中按动作拆分为多组时间和人员字段，也不等同于操作日志。
