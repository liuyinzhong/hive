# 疾病诊断档案 UI 规则

- 列表支持按 ICD 编码、名称、拼音和状态查询。
- 新增和修改使用抽屉；启停使用列表状态开关；删除需要二次确认。
- ICD 编码和名称共同维护同一条档案，页面不建立两套入口。
- 唯一性、启停和软删除规则以后端为准；失败时保留表单或列表行。
- 页面不提供 Excel 导入、疾病分类树、同义词或 ICD 版本切换。
- 医生工作台的诊断选择只加载启用档案，保存的是选择项和快照，不允许直接输入任意诊断。
