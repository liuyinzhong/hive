# 挂号费 UI 规则

## 列表

- 列表按医生、科室、挂号类型和生效状态筛选，状态展示由日期计算为当前、未来或已过期。
- 新增和调价/编辑使用抽屉；历史版本保留在列表中，不用前端覆盖成单条当前记录。

## 表单联动

- 先选择启用医生，再只展示该医生启用且开放预约的出诊科室。
- 挂号类型来自启用字典，金额大于零且最多两位小数。
- 生效开始日不能早于今天，结束日不能早于开始日。
- 调价时医生、科室和挂号类型只读，只维护新金额和生效期间；维度变化通过新增规则完成。
- 时间重叠冲突以后端为准，失败时保留输入并提示用户调整日期。
